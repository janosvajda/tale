Before working on this project, read .tale/project.tale in full (relative to the repository root). If it is missing or unreadable, report the problem before implementation. Discuss unclear requirements and agree on scope and completion checks before editing; existing explicit approval remains valid. The project context in the Tale does not authorize implementing every feature. Preserve user changes and deleted implementations. Stop when the agreed checks pass and report what was actually verified.
